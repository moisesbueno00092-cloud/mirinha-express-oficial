rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // This rule allows any authenticated user to read and write to any document in any collection.
      // Security is enforced at the application level by querying for documents with the correct userId.
      allow read, write: if request.auth != null;
    }
  }
}
